#!/bin/sh
#sudo dpkg-reconfigure tzdata

#sudo apt-get update && apt-get -y install puppet
#svn co http://knowledgeforge.net/kforge/svn/trunk kforge

# now done in firstboot
#sh install_start.sh >install_start.log 2>install_start.err
#sh localefix.sh  >localefix.log 2>localefix.log

#echo "Don't forget to sh appendrclocal.sh <color>!"

wget --user=jowolf --ask-password http://osdf.net/librepuppet/svn/chef/brewpub_bootstrap-1204.sh

echo >>firstlogin.DONE
# works: chroot root echo >firstlogin2.DONE

echo OK ready to bash brewpub_bootstrap.sh
